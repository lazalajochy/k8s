kubectl is the cli for kubernetes

api server all the comunication inside kubernetes work via this api server

etcd is the db to storage the status of kubernetes (key-value)


scheduler select the place where the pod is gonna be placed


controller manager 

kubelet focus on running containers, runs on all nodes

